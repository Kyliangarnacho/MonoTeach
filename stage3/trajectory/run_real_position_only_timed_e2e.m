function e2e = run_real_position_only_timed_e2e(jsonPath)
%RUN_REAL_POSITION_ONLY_TIMED_E2E Frozen 2 mm Position-Only execution chain.
%
% This is the Stage 3.3 E2E baseline: real C920 triangle -> existing 2 mm
% resampling -> fresh Position-Only IK -> existing timing -> formal Quintic.
% It deliberately excludes strict Writing recovery and adds no planning logic.

    if nargin < 1, jsonPath = []; end
    if isempty(jsonPath)
        ikRun = run_real_resampled_task_path_ik_ab();
    else
        ikRun = run_real_resampled_task_path_ik_ab(jsonPath);
    end
    positionOnly = ikRun.resampled_position_only_result_set;
    positionSummary = ikRun.resampled_position_only_summary;
    assert_position_only_baseline(positionOnly, positionSummary);

    robotContext = load_robot_context('legacy5');
    timingConfig = default_timed_joint_trajectory_config(robotContext);
    timed = time_parameterize_ik_success_segments( ...
        positionOnly, timingConfig, robotContext);
    continuous = generate_quintic_continuous_joint_trajectory( ...
        timed, timingConfig, robotContext);
    assert_continuous_contract(timed, continuous, robotContext);
    geometry = validate_position_only_geometry(robotContext, continuous, positionOnly);

    e2e = struct();
    e2e.artifact_type = 'RealPositionOnlyTimedE2E';
    e2e.execution_baseline = 'position_only_29_of_29_real_2mm_triangle';
    e2e.source_ik_run = ikRun;
    e2e.robot_context = robotContext;
    e2e.timing_config = timingConfig;
    e2e.timed_joint_trajectory = timed;
    e2e.continuous_joint_trajectory = continuous;
    e2e.position_only_geometry = geometry;
    e2e.summary = struct( ...
        'position_only_success_point_count', positionSummary.success_point_count, ...
        'position_only_failure_count', positionSummary.failure_count, ...
        'execution_segment_count', numel(continuous.segments), ...
        'duration_s', continuous.segments(1).duration_s, ...
        'peak_velocity_rad_s', continuous.segments(1).max_observed_abs_velocity_rad_s, ...
        'peak_acceleration_rad_s2', continuous.segments(1).max_observed_abs_acceleration_rad_s2, ...
        'velocity_limits_satisfied', continuous.segments(1).velocity_limits_satisfied, ...
        'acceleration_limits_satisfied', continuous.segments(1).acceleration_limits_satisfied, ...
        'joint_limits_satisfied', continuous.segments(1).joint_limits_satisfied, ...
        'mean_fk_path_deviation_m', geometry.summary.mean_fk_path_deviation_m, ...
        'max_fk_path_deviation_m', geometry.summary.max_fk_path_deviation_m, ...
        'minimum_joint_limit_margin', geometry.summary.minimum_joint_limit_margin);
end


function assert_position_only_baseline(resultSet, summary)

    if ~isstruct(resultSet) || ~isfield(resultSet, 'segments') || ...
            ~isfield(resultSet, 'failures') || ~isstruct(summary) || ...
            summary.success_point_count ~= 29 || summary.failure_count ~= 0 || ...
            numel(resultSet.segments) ~= 1 || numel(resultSet.failures) ~= 0 || ...
            size(resultSet.segments(1).q_rad, 1) ~= 29
        error('MonoTeach:PositionOnlyE2EBaselineMismatch', ...
            'Timed E2E requires the frozen real 2 mm Position-Only 29/29 baseline.');
    end
end


function assert_continuous_contract(timed, continuous, context)

    if ~isstruct(timed) || ~isfield(timed, 'segments') || ...
            numel(timed.segments) ~= 1 || ~isstruct(continuous) || ...
            ~isfield(continuous, 'generation_method') || ...
            ~strcmp(string(continuous.generation_method), 'quintic_hermite_v1') || ...
            numel(continuous.segments) ~= 1
        error('MonoTeach:PositionOnlyE2ETrajectoryMismatch', ...
            'Timed E2E requires one Quintic V1 Position-Only execution segment.');
    end
    segment = continuous.segments(1);
    if any(~isfinite(segment.q_rad), 'all') || ...
            any(~isfinite(segment.qd_rad_s), 'all') || ...
            any(~isfinite(segment.qdd_rad_s2), 'all') || ...
            any(diff(segment.t_s) <= 0) || ...
            ~segment.velocity_limits_satisfied || ...
            ~segment.acceleration_limits_satisfied || ...
            ~segment.joint_limits_satisfied || ...
            ~isequal(size(segment.q_rad, 2), context.dof)
        error('MonoTeach:PositionOnlyE2EContinuousSafetyFailure', ...
            'Quintic Position-Only trajectory violates its frozen execution contract.');
    end
end


function geometry = validate_position_only_geometry(context, continuous, resultSet)

    segment = continuous.segments(1);
    targets = vertcat(resultSet.segments(1).results.target_xyz_m);
    referenceXYZ = interp1(segment.waypoint_t_s(:), targets, segment.t_s(:), 'linear');
    fkXYZ = zeros(size(segment.q_rad, 1), 3);
    for index = 1:size(segment.q_rad, 1)
        pose = getTransform(context.model, segment.q_rad(index, :), context.end_effector);
        fkXYZ(index, :) = pose(1:3, 4)';
    end
    deviation = vecnorm(fkXYZ - referenceXYZ, 2, 2);
    margin = min(segment.q_rad - context.joint_limits(:, 1)', ...
        context.joint_limits(:, 2)' - segment.q_rad);
    if any(~isfinite([referenceXYZ; fkXYZ]), 'all') || ...
            any(~isfinite(deviation)) || ...
            any(segment.q_rad < context.joint_limits(:, 1)' | ...
                segment.q_rad > context.joint_limits(:, 2)', 'all')
        error('MonoTeach:PositionOnlyE2EFKValidationFailure', ...
            'Position-Only E2E FK geometry or joint-limit evidence is invalid.');
    end
    geometry = struct();
    geometry.artifact_type = 'PositionOnlyContinuousTaskGeometryValidation';
    geometry.reference_method = ...
        'piecewise_linear_position_only_ik_targets_at_quintic_waypoint_times';
    geometry.desired_waypoint_xyz_m = targets;
    geometry.t_s = segment.t_s;
    geometry.reference_xyz_m = referenceXYZ;
    geometry.fk_xyz_m = fkXYZ;
    geometry.position_deviation_m = deviation;
    geometry.joint_limit_margin = margin;
    geometry.summary = struct( ...
        'mean_fk_path_deviation_m', mean(deviation), ...
        'max_fk_path_deviation_m', max(deviation), ...
        'minimum_joint_limit_margin', min(margin, [], 'all'));
end
