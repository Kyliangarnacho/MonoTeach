function evidence = detect_joint_interval_overshoot( ...
        tS, qRad, waypointTS, waypointQRad, numericalEpsilonRad)
%DETECT_JOINT_INTERVAL_OVERSHOOT Detect sampled q(t) outside waypoint envelopes.
%
% Each interval/joint is compared only against the closed envelope formed by
% its adjacent position waypoints.  Values within numericalEpsilonRad are
% retained as in-envelope numerical noise rather than labelled overshoot.

    if nargin < 5 || isempty(numericalEpsilonRad)
        numericalEpsilonRad = 1.0e-8;
    end
    validate_inputs(tS, qRad, waypointTS, waypointQRad, numericalEpsilonRad);
    records = repmat(empty_record(), 1, 0);
    for intervalIndex = 1:numel(waypointTS)-1
        timeMask = tS >= waypointTS(intervalIndex) & ...
            tS <= waypointTS(intervalIndex + 1);
        intervalTimes = tS(timeMask);
        intervalQ = qRad(timeMask, :);
        for jointIndex = 1:5
            lower = min(waypointQRad(intervalIndex, jointIndex), ...
                waypointQRad(intervalIndex + 1, jointIndex));
            upper = max(waypointQRad(intervalIndex, jointIndex), ...
                waypointQRad(intervalIndex + 1, jointIndex));
            lowerViolation = lower - intervalQ(:, jointIndex);
            upperViolation = intervalQ(:, jointIndex) - upper;
            [lowerMagnitude, lowerIndex] = max(lowerViolation);
            [upperMagnitude, upperIndex] = max(upperViolation);
            if max(lowerMagnitude, upperMagnitude) <= numericalEpsilonRad
                continue;
            end
            if lowerMagnitude >= upperMagnitude
                magnitude = lowerMagnitude;
                sampleIndexInInterval = lowerIndex;
                direction = 'below_lower';
            else
                magnitude = upperMagnitude;
                sampleIndexInInterval = upperIndex;
                direction = 'above_upper';
            end
            records(end + 1) = struct( ...
                'interval_index', intervalIndex, 'joint_index', jointIndex, ...
                'lower_q_rad', lower, 'upper_q_rad', upper, ...
                'overshoot_magnitude_rad', magnitude, ...
                'sample_time_s', intervalTimes(sampleIndexInInterval), ...
                'sample_q_rad', intervalQ(sampleIndexInInterval, jointIndex), ...
                'direction', direction); %#ok<AGROW>
        end
    end
    evidence = struct('numerical_epsilon_rad', numericalEpsilonRad, ...
        'records', records, 'count', numel(records), ...
        'has_overshoot', ~isempty(records), ...
        'max_overshoot_magnitude_rad', 0.0);
    if ~isempty(records)
        evidence.max_overshoot_magnitude_rad = max([records.overshoot_magnitude_rad]);
    end
end


function validate_inputs(tS, qRad, waypointTS, waypointQRad, epsilon)

    if ~isnumeric(tS) || ~isreal(tS) || ~isvector(tS) || ...
            numel(tS) < 2 || any(~isfinite(tS)) || any(diff(tS(:)) <= 0) || ...
            ~isnumeric(qRad) || ~isreal(qRad) || ...
            ~isequal(size(qRad), [numel(tS), 5]) || any(~isfinite(qRad), 'all') || ...
            ~isnumeric(waypointTS) || ~isreal(waypointTS) || ...
            ~isvector(waypointTS) || numel(waypointTS) < 2 || ...
            any(~isfinite(waypointTS)) || any(diff(waypointTS(:)) <= 0) || ...
            ~isnumeric(waypointQRad) || ~isreal(waypointQRad) || ...
            ~isequal(size(waypointQRad), [numel(waypointTS), 5]) || ...
            any(~isfinite(waypointQRad), 'all') || ...
            tS(1) > waypointTS(1) || tS(end) < waypointTS(end) || ...
            ~isnumeric(epsilon) || ~isscalar(epsilon) || ...
            ~isfinite(epsilon) || epsilon < 0
        error('MonoTeach:InvalidJointOvershootInput', ...
            'Overshoot checking needs finite 5-DOF samples and increasing waypoint times.');
    end
end


function record = empty_record()

    record = struct('interval_index', NaN, 'joint_index', NaN, ...
        'lower_q_rad', NaN, 'upper_q_rad', NaN, ...
        'overshoot_magnitude_rad', NaN, 'sample_time_s', NaN, ...
        'sample_q_rad', NaN, 'direction', '');
end
