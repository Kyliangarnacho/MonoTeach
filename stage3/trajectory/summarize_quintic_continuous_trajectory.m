function summary = summarize_quintic_continuous_trajectory( ...
        continuousTrajectory, geometryValidation)
%SUMMARIZE_QUINTIC_CONTINUOUS_TRAJECTORY Pre-transition Quintic V1 summary.
%
% Joins a formal Quintic ContinuousJointTrajectory with its independent FK
% validation by segment identity only.  Durations remain local and independent:
% this helper deliberately does not construct a global execution timeline.

    validate_inputs(continuousTrajectory, geometryValidation);
    records = repmat(empty_record(), 1, numel(continuousTrajectory.segments));
    for index = 1:numel(continuousTrajectory.segments)
        trajectorySegment = continuousTrajectory.segments(index);
        validationIndex = find_validation_segment( ...
            geometryValidation.segments, trajectorySegment);
        geometrySegment = geometryValidation.segments(validationIndex);
        records(index) = record_for_segment(trajectorySegment, geometrySegment);
    end
    summary = struct();
    summary.artifact_type = 'QuinticContinuousTrajectoryPreTransitionSummary';
    summary.execution_timeline_exists = false;
    summary.duration_semantics = ...
        'sum_of_independent_local_ik_success_segment_durations_not_global_execution_time';
    summary.segment_records = records;
    summary.ik_success_segment_count = numel(records);
    summary.total_accepted_waypoint_count = sum([records.waypoint_count]);
    summary.total_independent_quintic_duration_s = sum([records.final_duration_s]);
    summary.all_velocity_limits_satisfied = all([records.velocity_limits_satisfied]);
    summary.all_acceleration_limits_satisfied = all( ...
        [records.acceleration_limits_satisfied]);
    summary.all_joint_limits_satisfied = all([records.joint_limits_satisfied]);
    summary.all_safety_checks_satisfied = all([records.all_safety_checks_satisfied]);
    summary.worst_cartesian = extremum_record(records, ...
        [records.cartesian_max_deviation_m], @max);
    summary.worst_tool_direction = extremum_record(records, ...
        [records.max_tool_direction_error_deg], @max);
    summary.smallest_joint_limit_margin = extremum_record(records, ...
        [records.minimum_joint_limit_margin_rad], @min);
end


function record = record_for_segment(trajectorySegment, geometrySegment)

    record = empty_record();
    record.segment_index = trajectorySegment.segment_index;
    record.ik_success_segment_index = trajectorySegment.ik_success_segment_index;
    record.preik_segment_index = trajectorySegment.preik_segment_index;
    record.waypoint_count = size(trajectorySegment.waypoint_q_rad, 1);
    record.continuous_sample_count = numel(trajectorySegment.t_s);
    record.initial_duration_s = trajectorySegment.initial_duration_s;
    record.final_duration_s = trajectorySegment.duration_s;
    record.time_stretch_factor = trajectorySegment.time_stretch_factor;
    record.time_stretch_iterations = trajectorySegment.time_stretch_iterations;
    record.peak_velocity_rad_s = trajectorySegment.max_observed_abs_velocity_rad_s;
    record.peak_acceleration_rad_s2 = ...
        trajectorySegment.max_observed_abs_acceleration_rad_s2;
    record.max_joint_overshoot_rad = ...
        trajectorySegment.overshoot_evidence.max_overshoot_magnitude_rad;
    record.minimum_joint_limit_margin_rad = ...
        trajectorySegment.minimum_joint_limit_margin;
    record.cartesian_mean_deviation_m = geometrySegment.mean_position_deviation_m;
    record.cartesian_max_deviation_m = geometrySegment.max_position_deviation_m;
    record.max_tool_direction_error_deg = ...
        geometrySegment.max_tool_direction_error_deg;
    record.velocity_limits_satisfied = trajectorySegment.velocity_limits_satisfied;
    record.acceleration_limits_satisfied = ...
        trajectorySegment.acceleration_limits_satisfied;
    record.joint_limits_satisfied = trajectorySegment.joint_limits_satisfied;
    record.all_safety_checks_satisfied = ...
        trajectorySegment.all_safety_checks_satisfied;
end


function value = extremum_record(records, measurements, reducer)

    [measurement, index] = reducer(measurements);
    value = struct('segment_index', records(index).segment_index, ...
        'ik_success_segment_index', records(index).ik_success_segment_index, ...
        'value', measurement);
end


function index = find_validation_segment(validationSegments, trajectorySegment)

    matching = find([validationSegments.segment_index] == trajectorySegment.segment_index & ...
        [validationSegments.ik_success_segment_index] == ...
        trajectorySegment.ik_success_segment_index);
    if numel(matching) ~= 1
        error('MonoTeach:QuinticSummarySegmentMismatch', ...
            'Every Quintic segment must have exactly one matching FK validation segment.');
    end
    index = matching;
end


function validate_inputs(continuous, validation)

    if ~isstruct(continuous) || ~isscalar(continuous) || ...
            ~isfield(continuous, 'artifact_type') || ...
            ~strcmp(string(continuous.artifact_type), "ContinuousJointTrajectory") || ...
            ~isfield(continuous, 'generation_method') || ...
            ~strcmp(string(continuous.generation_method), "quintic_hermite_v1") || ...
            ~isfield(continuous, 'segments') || ~isstruct(continuous.segments) || ...
            isempty(continuous.segments) || ~isstruct(validation) || ...
            ~isfield(validation, 'artifact_type') || ...
            ~strcmp(string(validation.artifact_type), ...
            "ContinuousJointTrajectoryTaskGeometryValidation") || ...
            ~isfield(validation, 'segments') || ~isstruct(validation.segments)
        error('MonoTeach:InvalidQuinticPreTransitionSummaryInput', ...
            'Summary requires formal Quintic V1 and matching FK validation artifacts.');
    end
    requiredTrajectory = {'segment_index', 'ik_success_segment_index', ...
        'preik_segment_index', 'waypoint_q_rad', 't_s', 'initial_duration_s', ...
        'duration_s', 'time_stretch_factor', 'time_stretch_iterations', ...
        'max_observed_abs_velocity_rad_s', ...
        'max_observed_abs_acceleration_rad_s2', 'overshoot_evidence', ...
        'minimum_joint_limit_margin', 'velocity_limits_satisfied', ...
        'acceleration_limits_satisfied', 'joint_limits_satisfied', ...
        'all_safety_checks_satisfied'};
    requiredGeometry = {'segment_index', 'ik_success_segment_index', ...
        'mean_position_deviation_m', 'max_position_deviation_m', ...
        'max_tool_direction_error_deg'};
    for index = 1:numel(continuous.segments)
        segment = continuous.segments(index);
        if ~all(isfield(segment, requiredTrajectory)) || ...
                ~isfield(segment.overshoot_evidence, ...
                'max_overshoot_magnitude_rad')
            error('MonoTeach:InvalidQuinticPreTransitionSegment', ...
                'Quintic segment %d does not satisfy the frozen V1 contract.', index);
        end
    end
    for index = 1:numel(validation.segments)
        if ~all(isfield(validation.segments(index), requiredGeometry))
            error('MonoTeach:InvalidQuinticPreTransitionValidation', ...
                'FK validation segment %d is incomplete.', index);
        end
    end
end


function record = empty_record()

    record = struct('segment_index', NaN, 'ik_success_segment_index', NaN, ...
        'preik_segment_index', NaN, 'waypoint_count', NaN, ...
        'continuous_sample_count', NaN, 'initial_duration_s', NaN, ...
        'final_duration_s', NaN, 'time_stretch_factor', NaN, ...
        'time_stretch_iterations', NaN, 'peak_velocity_rad_s', NaN(1, 5), ...
        'peak_acceleration_rad_s2', NaN(1, 5), 'max_joint_overshoot_rad', NaN, ...
        'minimum_joint_limit_margin_rad', NaN, ...
        'cartesian_mean_deviation_m', NaN, 'cartesian_max_deviation_m', NaN, ...
        'max_tool_direction_error_deg', NaN, ...
        'velocity_limits_satisfied', false, 'acceleration_limits_satisfied', false, ...
        'joint_limits_satisfied', false, 'all_safety_checks_satisfied', false);
end
